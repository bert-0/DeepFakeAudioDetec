# Dependências de desenvolvimento (testes). Instale junto com requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.0
ruff>=0.5           # mesmas regras que o CI aplica: ruff check --select E9,F .
