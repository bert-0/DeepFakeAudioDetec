# Pipeline de IA — Detecção de Deepfakes em Áudio
# Testado com Python 3.10+

torch>=2.0
numpy>=1.23
scipy>=1.10
librosa>=0.10
soundfile>=0.12      # backend de leitura de .flac (ASVspoof) para o librosa
scikit-learn>=1.2
matplotlib>=3.6
pyyaml>=6.0
tqdm>=4.64
threadpoolctl>=3.0   # limita threads BLAS nos workers do DataLoader

# Opcional — só para o monitor de chamada ao vivo (monitor.py).
# No Windows dá acesso ao loopback WASAPI, que captura a saída do sistema.
# Não é necessário para treinar nem avaliar.
# soundcard>=0.4
